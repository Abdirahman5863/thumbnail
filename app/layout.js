
export const metadata = {
  title: "thumnail",
  description: "Create youtube  thumnail ",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
      >
        {children}
      </body>
    </html>
  );
}
